<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="80dp"
    android:layout_marginBottom="12dp"
    android:background="@drawable/moments_card_bg"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="28dp"
    android:paddingEnd="12dp">

    <ImageView
        android:id="@+id/moment_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:importantForAccessibility="no" />

    <TextView
        android:id="@+id/moment_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="28dp"
        android:layout_weight="1"
        android:ellipsize="end"
        android:maxLines="1"
        android:textSize="20sp" />

    <ImageView
        android:id="@+id/moment_edit"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:background="@drawable/moments_circle_bg"
        android:contentDescription="@string/moments_edit"
        android:padding="16dp"
        android:src="@drawable/ic_moment_tune" />
</LinearLayout>
